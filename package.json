{
  "name": "@david.uhlir/cluster",
  "version": "2.0.3",
  "description": "Cluster",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "prettier-format": "prettier --config .prettierrc.json 'lib/**/*.ts' --write"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/daviduhlir/cluster.git"
  },
  "keywords": [
    "cluster",
    "workers",
    "node",
    "util"
  ],
  "author": "David Uhlíř",
  "license": "ISC",
  "dependencies": {
    "@david.uhlir/ipc-method": "^1.0.12",
    "prettier": "^2.6.2",
    "rimraf": "^2.6.2",
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "@types/node": "^13.11.1"
  }
}
